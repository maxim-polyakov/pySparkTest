# Скопируйте в корень проекта: pySparkTest/.env (рядом с docker-compose.yml)
AIRFLOW_UID=0
AIRFLOW_FERNET_KEY=
AIRFLOW_HOST_PROJECT_DIR=D:/GitHub/pySparkTest

# FERNET_KEY:
# docker run --rm apache/airflow:2.10.5-python3.11 python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#
# Если docker exec из Airflow падает (permission denied на Windows):
# AIRFLOW_UID=0
